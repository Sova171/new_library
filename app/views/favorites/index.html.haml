%h5.align-center= t('.welcome')
- if @favorite_books.blank?
  %h6.align-center= t('.empty')
= render "shared/books_card", books: @favorite_books do |book|
  = link_to t('.delete'), book_favorites_path(book_id: book), method: :delete

